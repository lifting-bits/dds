#import "header.dr"

;---------------------------------------------------------
get_section_names(SecName) 
    : section(SecName, _, _).

;---------------------------------------------------------
section_start(SecStart)
    : section(_, SecStart, _).
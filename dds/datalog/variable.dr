#import "header.dr"

;---------------------------------------------------------
; Return all imported, exported, and local variable addresses.
#query get_variables(free u64 EA)

get_variables(EA)
    : imported_variable(EA, _).

get_variables(EA)
	: exported_variable(EA, _).

get_variables(EA)
	: local_variable(EA, _).